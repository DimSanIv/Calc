# Calc — Калькулятор в стиле OS/2

## Функционал

- Сложение, вычитание, умножение, деление и процент с точностью `Decimal`.
- Интерфейс в стиле OS/2 (IBM): серый фон, крупные кнопки операций.
- Окно «Текущая сумма» и поле «Ввод», лента вычислений в отдельном окне.
- Управление лентой: «Очистить ленту», «Пересчитать», «Скрыть» (скрыть/показать окно истории).
- Лента редактируемая: можно менять числа в строках; изменённые строки подсвечиваются красным. По кнопке «Пересчитать» цепочка пересчитывается, лента и **calc.log** обновляются. Отрицательные числа на ленте отображаются корректно.
- Лента сохраняется в файл **calc.log** в папке программы; при запуске лента подгружается из этого файла. Кнопка «Очистить ленту» очищает и окно ленты, и содержимое **calc.log**.
- Кнопки: цифры 0–9, «Очистить», «←» (backspace), «+», «-», «*», «/», «%», «=».
- Поддержка клавиатуры: цифры, +, -, *, /, %, Enter, BackSpace, Delete, C (очистка), запятая/точка для дробной части.
- Процент (\%): введённое число заменяется на «текущая_сумма × число / 100» (например: 100, +, 15, %, = → 115).

## Запуск

Только стандартная библиотека Python (tkinter входит в поставку Python).

```bash
cd Calc
python calc.py
```

Окно «Лента вычислений» открывается рядом с калькулятором; закрытие крестиком только скрывает его (показать снова — через кнопку в основном окне).
